<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ke.co.example.weaversoft.crbclientregister" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AddClientScreen"
            android:label="Add New Client"
            android:theme="@style/AppTheme"/>

        <activity android:name=".ViewClientScreen"
            android:label="View Client Details"
            android:theme="@style/AppTheme"/>

        <activity android:name=".EditClientScreen"
            android:label="Edit Client Details"
            android:theme="@style/AppTheme"/>

        <activity android:name=".ViewNextOfKin"
            android:label="Next of Kin List"
            android:theme="@style/AppTheme"/>

        <activity android:name=".AddNextOfKinScreen"
            android:label="Add New Next of Kin"
            android:theme="@style/AppTheme"/>

        <activity android:name=".EditNextOfKinScreen"
            android:label="Edit Next of Kin"
            android:theme="@style/AppTheme"/>

    </application>

</manifest>
